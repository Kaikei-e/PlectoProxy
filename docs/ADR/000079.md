---
title: release に named runtime capability profile を導入し、ソースビルドのみを production floor としない
date: 2026-07-12
status: accepted
tags: [distribution, capability, security, ci-cd, decision]
affected_components:
  - CI / release — default-feature のみの単一成果物前提を見直し、named capability profile の prebuilt 成果物と support matrix を導入する
  - host / server — `outbound-http` / `outbound-tcp` の compile-time 含有と runtime grant の境界は維持
  - docs — profile 名、README 既定導線、upgrade policy を一級文書化する
aliases: ["ADR-79", "ADR-000079"]
amends: ["000047"]
---

# release に named runtime capability profile を導入し、ソースビルドのみを production floor としない

## Status
accepted（2026-07-12 実装前 grill で Decision 3 が留保した選択を解消し、同日実装。確定内容は Decision 末尾「実装前 grill による確定」）

## Date
2026-07-12

## Affected Components
- CI / release（`.github/workflows/release.yml` ほか）— 現行は `cargo auditable build --release --locked -p plecto-server` で feature 指定なし＝default のみ。本 ADR 採択後、named profile の prebuilt 成果物を定義する
- host — feature `outbound-http` / `outbound-tcp` の off-by-default 隔離（[[000036]] / [[000060]]）は維持。昇格の是非は本 ADR の Decision で枠を固定
- docs / README — どの profile を試す導線の既定にするかを明示

## Context

需要側が global rate limit reference（outbound-tcp）や JWKS / ext-authz（outbound-http）を試すには、capability 付きランタイムが手に入る必要がある。確定事実（2026-07-12）:

1. **出荷バイナリは default features のみ**であり、当該 reference はソースビルド + feature flag 無しでは動かない。
2. **Cargo の features は additive であるべき**であり、組合せはテスト構成を指数的に増やす（Cargo Book Features 章）。複数 profile は「小さい packaging 変更」ではなく support matrix 案件である。
3. **compile-time にコードが含まれることと、runtime に capability が貸与されることは別境界**である。後者は引き続き manifest の deny-by-default + allowlist + SSRF 床（[[000036]] / [[000060]]）が強制する。

[[000047]] は署名付きバイナリ・SBOM・cosign を自らの配布物に課した。capability 無しの単一成果物のままでは、供給網規律を満たしても**需要側カタログの蛇口が閉じたまま**になる。

## Decision

1. **ソースビルド手順の一級文書化だけを production floor としない。** technical preview（[[000077]]）および production-readiness の需要側主張には、**prebuilt な capability 付き runtime**が必要である。

2. **named runtime capability profile を導入する。** 少なくとも次を区別して名付ける（最終名は実装時に確定してよい）:
   - **minimal** — 現行 default に相当。outbound 系を compile しない（または含めない）最小攻撃面。
   - **capabilities** — `outbound-http` と `outbound-tcp` を compile-time に含む profile。実行時は従来どおり filter 単位の宣言と SSRF 床が全て効く。

3. **単一 canonical 成果物か dual 成果物かは、実装着手前の grill で support matrix コストを見て選ぶ。** 本 ADR が禁止するのは次のみ:
   - 議論なしに default features へ outbound を silently 昇格すること（[[000036]] / [[000060]] の隔離根拠を黙って覆すこと）。
   - profile を増やしたのに test / clippy / SBOM / cosign / vulnerability response を minimal だけに残すこと。

4. **各 profile は同一の供給網規律（[[000047]]）に服する。** auditable build、SBOM、cosign、draft release 経由の公開判断。profile ごとに digest を追跡する。

5. **compile-time inclusion ≠ runtime grant を文書の一等市民にする。** capabilities profile のバイナリを配ることと、任意 filter が任意ホストへ出られることを同義にしてはならない。

検討した代替案と却下理由:

- **ソースビルド手順だけを床とする** — 却下。需要側ペルソナに Rust toolchain とビルド時間を強いる。
- **default features へ即昇格** — 却下（現時点）。ビルド隔離の根拠（攻撃面・依存量）を覆すには [[000036]] / [[000060]] を正面から amend する論証が要る。grill なしの昇格は禁止。
- **profile 名だけ決めて成果物は一つ・ドキュメントで feature を指示** — 却下。prebuilt が無い点でソースビルド床と同型。
- **HTTP と TCP で別々の第三・第四成果物を最初から増やす** — 却下。組合せ爆発を早める。まずは outbound 系を束ねた capabilities profile を単位にする。

本決定は [[000047]] の供給網規律を拡張し、[[000036]] / [[000060]] の capability 境界は維持する。P4（deny-by-default）と運用の単純さの緊張を、profile という明示選択で扱う。

### 実装前 grill による確定（2026-07-12）

Decision 3 が grill に委ねた選択と、Decision 2 の「最終名は実装時に確定してよい」を次のとおり確定した。

1. **dual 成果物（minimal + capabilities）を採用する。** release workflow は profile × arch の matrix 駆動で、増分は機械的（binaries 2→4 job、container image 2 系統）。最小攻撃面バイナリを失わず、[[000036]] / [[000060]] の隔離根拠を amend なしで保存する。縮退条件は Cons の再検討条件 (a) を維持。
2. **capabilities profile の中身は `outbound-http` + `outbound-tcp` + `fat-guest`。** fat-guest（[[000063]]）の依存は outbound 系が既に引く集合の部分集合で供給網の純増ゼロ、かつ prebuilt で Tier B（TinyGo/Go）guest が動く。experimental の streaming-body はどの prebuilt profile にも含めない。cargo feature 名 = profile 名（plecto-server の umbrella feature `capabilities`）とし、profile 名とビルドフラグの drift を構造的に防ぐ。
3. **無印 = minimal。** tar.gz（`plecto-<tag>-<target>.tar.gz`）と image tag（`<version>` / `<major>.<minor>`）の無印は minimal を意味し、capabilities は `-capabilities` suffix。「何も指定しなければ最小攻撃面」という deny-by-default の姿勢を配布名にも延長する（variant を tag suffix で表すのはコンテナ配布の確立した慣行）。
4. **docs の既定導線は用途で分岐する。** quickstart / 一般 proxy 用途は minimal、capability 依存 reference（JWKS / ext-authz / global rate limit）の導線は capabilities を明示指名。compile-time inclusion ≠ runtime grant の注記を両導線に置く（README「Prebuilt binaries & container images」）。
5. **CI 規律（Decision 3 の禁止事項の履行）。** それまで CI が一度も実行していなかった outbound-http のテスト（extauthz / jwt）を feature combo に加えて穴を塞ぐ。capabilities の feature 配線は毎 PR の `cargo check` で検証し（型レベルの配線破壊はここでほぼ全て捕まる。取り逃すのはリンク / codegen 起因のみ — RFC 3477）、release-profile ビルドは main push のみに置く。release.yml の gate はタグ付きコミットの CI green を要求するため、タグ時点の release-parity は成立し、PR の wall-time は平坦に保たれる。
6. **バイナリは profile を自己申告する。** `plecto --version` が compile-time の capability feature 集合を named profile として出力する（部分集合の source build は named profile を僭称せず feature 列挙で報告する）。Decision 5 の一等市民化の実装。
7. **per-profile digest 追跡。** container-merge が両 profile の multi-arch index digest を draft release notes に記録する（Decision 4 の履行）。

## Consequences

### Pros
- 需要側が「動くバイナリ」で capability 依存 reference に届く。
- minimal を残せる選択を捨てない（隔離根拠の保存）。
- テスト / 署名 / SBOM の matrix を意識せざるを得なくなり、条件付きコンパイルの暗黙負債が可視化される。

### Cons / Tradeoffs
- dual を選ぶとリリース資産・CVE 応答・ドキュメント導線が倍近くになる。
- single capabilities canonical を選ぶと最小攻撃面バイナリを失う（または別チャネルへ退避）。
- **再検討条件**: (a) grill で matrix コストが単独開発の帯域を超えると判明したとき、capabilities のみの単一成果物へ縮退する；(b) outbound 系の依存 CVE が持続的に minimal 利用者へ無関係な騒音を与えるとき dual を強制する；(c) WASI / host 側の能力モデルが変わり feature 境界そのものが不要になったとき、本 ADR を再評価する。

## Related ADRs
- [[000036]] filter に outbound HTTP capability を deny-by-default ＋ allowlist ＋ SSRF ガードで貸与する
- [[000047]] フィルタに課す供給網規律を自らの配布物に適用し、署名付きバイナリ・イメージで v0.1.0 を切る（本 ADR が成果物形を amend）
- [[000060]] filter に outbound TCP capability を deny-by-default ＋ allowlist ＋ SSRF ガードで貸与する
- [[000061]] レートリミットを local floor × global filter の二層モデルとして完成させ、Redis reference filter を提供する
- [[000063]] fat guest に minimal WASI slice を貸与する（capabilities profile が同梱する第三の feature）
- [[000077]] 発信を段階化し、外部 TTFVF を hard gate から供給側 DX 指標へ格下げする
- [[000080]] reference filter を署名付き OCI artifact として runtime とは別に配布する
- `docs/servey/plecto-strategy-v1.3.2-demand-floor-2026-07-11.md`
- `CLAUDE.md` コア原則（安全 × セルフホスト性 × 運用の単純さ）
