// Generated by `wit-bindgen` 0.58.0. DO NOT EDIT!

#doc(hidden)
pub fn wasmExportInit() -> Unit {

      let _ = init_();

}

#doc(hidden)
pub fn wasmExportOnRequest(p0 : Int, p1 : Int, p2 : Int, p3 : Int, p4 : Int, p5 : Int, p6 : Int, p7 : Int, p8 : Int, p9 : Int) -> Int {

      let result = mbt_ffi_ptr2str(p0, p1)

      let result0 = mbt_ffi_ptr2str(p2, p3)

      let result1 = mbt_ffi_ptr2str(p4, p5)

      let result2 = mbt_ffi_ptr2str(p6, p7)

      let array : Array[@types.Header] = [];
      for index = 0; index < (p9); index = index + 1 {
            let iter_base = (p8) + (index * 16)

            let result3 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

            let result4 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

            array.push(@types.Header::{name : result3, value : result4})
      }
      mbt_ffi_free(p8)

      let (result5) : (@types.RequestDecision) = on_request(@types.HttpRequest::{method_ : result, path : result0, authority : result1, scheme : result2, headers : array});
      let return_area = mbt_ffi_malloc(24)

      match result5 {
            Continue => {
                  mbt_ffi_store8((return_area) + 0, (0))

                  ()
            }
            Modified(payload6) => {
                  mbt_ffi_store8((return_area) + 0, (1))

                  let address = mbt_ffi_malloc(((payload6).set_headers).length() * 16);
                  for index8 = 0; index8 < ((payload6).set_headers).length(); index8 = index8 + 1 {
                        let iter_elem : @types.Header = ((payload6).set_headers)[(index8)]
                        let iter_base = address + (index8 * 16);

                        let ptr = mbt_ffi_str2ptr((iter_elem).name)
                        mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                        mbt_ffi_store32((iter_base) + 0, ptr)

                        let ptr7 = mbt_ffi_str2ptr((iter_elem).value)
                        mbt_ffi_store32((iter_base) + 12, (iter_elem).value.length())
                        mbt_ffi_store32((iter_base) + 8, ptr7)

                  }
                  mbt_ffi_store32((return_area) + 8, ((payload6).set_headers).length())
                  mbt_ffi_store32((return_area) + 4, address)

                  let address10 = mbt_ffi_malloc(((payload6).remove_headers).length() * 8);
                  for index11 = 0; index11 < ((payload6).remove_headers).length(); index11 = index11 + 1 {
                        let iter_elem : String = ((payload6).remove_headers)[(index11)]
                        let iter_base = address10 + (index11 * 8);

                        let ptr9 = mbt_ffi_str2ptr(iter_elem)
                        mbt_ffi_store32((iter_base) + 4, iter_elem.length())
                        mbt_ffi_store32((iter_base) + 0, ptr9)

                  }
                  mbt_ffi_store32((return_area) + 16, ((payload6).remove_headers).length())
                  mbt_ffi_store32((return_area) + 12, address10)

                  ()
            }
            ShortCircuit(payload12) => {
                  mbt_ffi_store8((return_area) + 0, (2))
                  mbt_ffi_store16((return_area) + 4, ((payload12).status).reinterpret_as_int())

                  let address15 = mbt_ffi_malloc(((payload12).headers).length() * 16);
                  for index16 = 0; index16 < ((payload12).headers).length(); index16 = index16 + 1 {
                        let iter_elem : @types.Header = ((payload12).headers)[(index16)]
                        let iter_base = address15 + (index16 * 16);

                        let ptr13 = mbt_ffi_str2ptr((iter_elem).name)
                        mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                        mbt_ffi_store32((iter_base) + 0, ptr13)

                        let ptr14 = mbt_ffi_str2ptr((iter_elem).value)
                        mbt_ffi_store32((iter_base) + 12, (iter_elem).value.length())
                        mbt_ffi_store32((iter_base) + 8, ptr14)

                  }
                  mbt_ffi_store32((return_area) + 12, ((payload12).headers).length())
                  mbt_ffi_store32((return_area) + 8, address15)

                  let ptr17 = mbt_ffi_bytes2ptr((payload12).body)

                  mbt_ffi_store32((return_area) + 20, (payload12).body.length())
                  mbt_ffi_store32((return_area) + 16, ptr17)

                  ()
            }
      }
      let ret = return_area
      return ret

}

#doc(hidden)
pub fn wasmExportOnRequestPostReturn(p0 : Int) -> Unit {

      match (mbt_ffi_load8_u((p0) + 0)) {
            0 => ()
            1 => {

                  for index = 0; index < (mbt_ffi_load32((p0) + 8)); index = index + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 4)) + (index * 16)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 4))

                  for index0 = 0; index0 < (mbt_ffi_load32((p0) + 16)); index0 = index0 + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 12)) + (index0 * 8)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 12))

            }
            2 => {

                  for index1 = 0; index1 < (mbt_ffi_load32((p0) + 12)); index1 = index1 + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 8)) + (index1 * 16)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 8))
                  mbt_ffi_free(mbt_ffi_load32((p0) + 16))

            }
            _ => panic()
      }

}

#doc(hidden)
pub fn wasmExportOnRequestBody(p0 : Int, p1 : Int) -> Int {

      let result = mbt_ffi_ptr2bytes(p0, p1)

      let (result0) : (@types.RequestBodyDecision) = on_request_body(result);
      let return_area = mbt_ffi_malloc(24)

      match result0 {
            Continue(payload) => {
                  mbt_ffi_store8((return_area) + 0, (0))

                  let ptr = mbt_ffi_bytes2ptr(payload)

                  mbt_ffi_store32((return_area) + 8, payload.length())
                  mbt_ffi_store32((return_area) + 4, ptr)

                  ()
            }
            ShortCircuit(payload1) => {
                  mbt_ffi_store8((return_area) + 0, (1))
                  mbt_ffi_store16((return_area) + 4, ((payload1).status).reinterpret_as_int())

                  let address = mbt_ffi_malloc(((payload1).headers).length() * 16);
                  for index = 0; index < ((payload1).headers).length(); index = index + 1 {
                        let iter_elem : @types.Header = ((payload1).headers)[(index)]
                        let iter_base = address + (index * 16);

                        let ptr2 = mbt_ffi_str2ptr((iter_elem).name)
                        mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                        mbt_ffi_store32((iter_base) + 0, ptr2)

                        let ptr3 = mbt_ffi_str2ptr((iter_elem).value)
                        mbt_ffi_store32((iter_base) + 12, (iter_elem).value.length())
                        mbt_ffi_store32((iter_base) + 8, ptr3)

                  }
                  mbt_ffi_store32((return_area) + 12, ((payload1).headers).length())
                  mbt_ffi_store32((return_area) + 8, address)

                  let ptr4 = mbt_ffi_bytes2ptr((payload1).body)

                  mbt_ffi_store32((return_area) + 20, (payload1).body.length())
                  mbt_ffi_store32((return_area) + 16, ptr4)

                  ()
            }
      }
      let ret = return_area
      return ret

}

#doc(hidden)
pub fn wasmExportOnRequestBodyPostReturn(p0 : Int) -> Unit {

      match (mbt_ffi_load8_u((p0) + 0)) {
            0 => {
                  mbt_ffi_free(mbt_ffi_load32((p0) + 4))

            }
            1 => {

                  for index = 0; index < (mbt_ffi_load32((p0) + 12)); index = index + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 8)) + (index * 16)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 8))
                  mbt_ffi_free(mbt_ffi_load32((p0) + 16))

            }
            _ => panic()
      }

}

#doc(hidden)
pub fn wasmExportOnResponse(p0 : Int, p1 : Int, p2 : Int, p3 : Int, p4 : Int) -> Int {

      let array : Array[@types.Header] = [];
      for index = 0; index < (p2); index = index + 1 {
            let iter_base = (p1) + (index * 16)

            let result = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 0), mbt_ffi_load32((iter_base) + 4))

            let result0 = mbt_ffi_ptr2str(mbt_ffi_load32((iter_base) + 8), mbt_ffi_load32((iter_base) + 12))

            array.push(@types.Header::{name : result, value : result0})
      }
      mbt_ffi_free(p1)

      let result1 = mbt_ffi_ptr2bytes(p3, p4)

      let (result2) : (@types.ResponseDecision) = on_response(@types.HttpResponse::{status : (p0.land(0xFFFF).reinterpret_as_uint()), headers : array, body : result1});
      let return_area = mbt_ffi_malloc(24)

      match result2 {
            Continue => {
                  mbt_ffi_store8((return_area) + 0, (0))

                  ()
            }
            Modified(payload3) => {
                  mbt_ffi_store8((return_area) + 0, (1))

                  match ((payload3).set_status) {
                        None => {
                              mbt_ffi_store8((return_area) + 4, (0))

                              ()
                        }
                        Some(payload5) => {
                              mbt_ffi_store8((return_area) + 4, (1))
                              mbt_ffi_store16((return_area) + 6, (payload5).reinterpret_as_int())

                              ()
                        }
                  }

                  let address = mbt_ffi_malloc(((payload3).set_headers).length() * 16);
                  for index7 = 0; index7 < ((payload3).set_headers).length(); index7 = index7 + 1 {
                        let iter_elem : @types.Header = ((payload3).set_headers)[(index7)]
                        let iter_base = address + (index7 * 16);

                        let ptr = mbt_ffi_str2ptr((iter_elem).name)
                        mbt_ffi_store32((iter_base) + 4, (iter_elem).name.length())
                        mbt_ffi_store32((iter_base) + 0, ptr)

                        let ptr6 = mbt_ffi_str2ptr((iter_elem).value)
                        mbt_ffi_store32((iter_base) + 12, (iter_elem).value.length())
                        mbt_ffi_store32((iter_base) + 8, ptr6)

                  }
                  mbt_ffi_store32((return_area) + 12, ((payload3).set_headers).length())
                  mbt_ffi_store32((return_area) + 8, address)

                  let address9 = mbt_ffi_malloc(((payload3).remove_headers).length() * 8);
                  for index10 = 0; index10 < ((payload3).remove_headers).length(); index10 = index10 + 1 {
                        let iter_elem : String = ((payload3).remove_headers)[(index10)]
                        let iter_base = address9 + (index10 * 8);

                        let ptr8 = mbt_ffi_str2ptr(iter_elem)
                        mbt_ffi_store32((iter_base) + 4, iter_elem.length())
                        mbt_ffi_store32((iter_base) + 0, ptr8)

                  }
                  mbt_ffi_store32((return_area) + 20, ((payload3).remove_headers).length())
                  mbt_ffi_store32((return_area) + 16, address9)

                  ()
            }
      }
      let ret = return_area
      return ret

}

#doc(hidden)
pub fn wasmExportOnResponsePostReturn(p0 : Int) -> Unit {

      match (mbt_ffi_load8_u((p0) + 0)) {
            0 => ()
            1 => {

                  for index = 0; index < (mbt_ffi_load32((p0) + 12)); index = index + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 8)) + (index * 16)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 8))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 8))

                  for index0 = 0; index0 < (mbt_ffi_load32((p0) + 20)); index0 = index0 + 1 {
                        let iter_base = (mbt_ffi_load32((p0) + 16)) + (index0 * 8)
                        mbt_ffi_free(mbt_ffi_load32((iter_base) + 0))

                  }
                  mbt_ffi_free(mbt_ffi_load32((p0) + 16))

            }
            _ => panic()
      }

}

///|
#owned(str)
extern "wasm" fn mbt_ffi_str2ptr(str : String) -> Int =
#|(func (param i32) (result i32) local.get 0)

///|
extern "wasm" fn mbt_ffi_store32(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store)

///|
#owned(bytes)
extern "wasm" fn mbt_ffi_bytes2ptr(bytes : FixedArray[Byte]) -> Int =
#|(func (param i32) (result i32) local.get 0)

///|
extern "wasm" fn mbt_ffi_free(position : Int) =
#|(func (param i32) local.get 0 call $moonbit.decref)

///|
extern "wasm" fn mbt_ffi_ptr2str(ptr : Int, len : Int) -> String =
#|(func (param i32) (param i32) (result i32)
#| local.get 0
#| local.get 1 call $moonbit.init_array16
#| local.get 0)

///|
extern "wasm" fn mbt_ffi_malloc(size : Int) -> Int =
#|(func (param i32) (result i32) (local i32)
#| local.get 0 i32.const 4 i32.add call $moonbit.gc.malloc
#| local.tee 1 i32.const 0 call $moonbit.init_array8
#| local.get 1)

///|
extern "wasm" fn mbt_ffi_ptr2bytes(ptr : Int, len : Int) -> FixedArray[Byte] =
#|(func (param i32) (param i32) (result i32)
#| local.get 0
#| local.get 1 call $moonbit.init_array8
#| local.get 0)

///|
extern "wasm" fn mbt_ffi_load32(offset : Int) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.load)

///|
extern "wasm" fn mbt_ffi_store16(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store16)

///|
extern "wasm" fn mbt_ffi_store8(offset : Int, value : Int) =
#|(func (param i32) (param i32) local.get 0 local.get 1 i32.store8)

///|
extern "wasm" fn mbt_ffi_load8_u(offset : Int) -> Int =
#|(func (param i32) (result i32) local.get 0 i32.load8_u)

